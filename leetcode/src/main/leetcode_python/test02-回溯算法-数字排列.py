str_list = []
def genrate(num_count):
    if 
    
    for key,value in num_count:
        num_count[key] = value-1
        if value>0:
            
        
    return 

nums = "acba"
sorted(nums)

num_count = dict()
for num in nums:
    if num in num_count:
        num_count[num] = num_count[num]+1
    else:
        num_count[num] = 1

num_count

genrate(num_count)


